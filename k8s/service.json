{
   "apiVersion":"v1",
   "kind":"Service",
   "metadata":{
      "name":"${JOB_NAME}-service",
      "labels":{
          "service":"${JOB_NAME}"
      }
   },
   "spec":{
       "selector":{
           "app": "${JOB_NAME}"
       },
       "ports":[
           {"port":"8080"}
       ],
       "type":"NodePort"
   }
}
